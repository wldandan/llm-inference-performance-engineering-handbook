# Chapter 5 Integration Report

## 已回填图片

- 图 5-1：GPU 的四类工程约束。
- 图 5-2：LLM 工作怎样落到 GPU 资源。
- 图 5-3：显存容量预算。
- 图 5-4：显存带宽与数据路径。
- 图 5-5：算力与 Roofline 直觉。
- 图 5-6：Kernel Launch Overhead。
- 图 5-7：进阶硬件地图。
- 图 5-8：Demo 输出 GPU 约束报告。

## 修改位置

- 图 5-1 位于四类预算定义之后。
- 图 5-2 位于模型工作与 GPU 资源映射之后。
- 图 5-3 位于权重、KV Cache 和安全余量公式之后。
- 图 5-4 位于 HBM 到片上存储的数据路径说明之后。
- 图 5-5 位于算术强度与 Ridge Point 说明之后。
- 图 5-6 位于长短 Kernel Timeline 对比之后。
- 图 5-7 位于进阶选修名词说明之后。
- 图 5-8 位于离线预算报告字段说明之后。

## 图号与路径检查

正文只包含图 5-1 到图 5-8；8 个 SVG 路径、8 份 figure-note、Storyboard 文件名和视觉测试定义一致。

## 图文一致性

- 图 5-1 的四个分支分别回答计算、容量、带宽和提交问题。
- 图 5-3 将 Reserve 画成不可挤占区域，不把 GPU Memory 全部当作可用预算。
- 图 5-4 同时表现读取与写回，不把 HBM 画成计算单元。
- 图 5-5 是概念 Roofline，不含伪造实测点。
- 图 5-6 只比较固定开销的相对占比，不宣称某个确定 Launch 延迟。
- 图 5-7 标为 Advanced Track，Occupancy 明确不是性能总分。
- 图 5-8 的模式名与实际 JSON 一致，并保留 `NOT A BENCHMARK`。

## 未解决问题

尚未在课程指定 GPU 上采集真实有效带宽、Kernel Timeline 和显存水位，因此本章只交付可复现的离线预算器与验证框架。

## 验收结果

正文、Storyboard、SVG、figure-note、Demo、图号和路径已经对齐；单元测试、视觉契约、XML 解析和 1280×720 原尺寸检查通过。
