# 《LLM 服务与优化实战》Top 30 Q&A

> 本文档根据《LLM 服务与优化实战》（Chi Wang、Peiheng Hu）全书内容整理。问题按“最值得掌握、最容易影响工程决策”的原则筛选，不是原书官方习题。

## 一、基础概念与执行机制

### 1. 为什么 LLM 服务不能简单照搬传统模型服务？

LLM 通常具有自回归生成、输入输出长度差异大、上下文窗口长、推理持续时间长以及需要流式输出等特征。传统图像分类等模型往往是固定形状、短输入、一次前向就结束；LLM 则要经历预填充和逐 token 解码，且 KV 缓存会随序列增长。因此，LLM 服务必须额外处理 token 级调度、动态批处理、KV 缓存管理和流式传输。

### 2. LLM 推理中的预填充（Prefill）和解码（Decode）分别做什么？

预填充处理完整输入提示，建立模型对上下文的表示并生成初始 KV 缓存；输入 token 可以并行处理，通常更偏计算受限。解码根据已有上下文逐步生成输出 token，每一步通常只新增一个 token，反复读取模型权重并更新 KV 缓存，通常更偏内存带宽受限。

### 3. KV 缓存是什么？为什么能加速生成？

KV 缓存保存注意力层已经计算过的 key/value。生成下一个 token 时，模型只需计算新 token 的 query、key、value，并复用历史 token 的 key/value，不必重新计算整个序列。它把“每一步重算全序列”变成“增量计算”，显著降低解码延迟；代价是需要占用大量 GPU 显存，并且缓存管理会成为服务系统的重要瓶颈。

### 4. 为什么 LLM 服务需要流式输出？

LLM 可能需要数秒甚至更久才能生成完整回答。流式输出允许服务在首个 token 生成后立即返回，并持续发送后续 token，从而降低用户感知等待时间。它主要改善首 token 体验，并不等同于降低完整响应的端到端延迟；系统仍需关注 TTFT、token 间延迟和最终完成时间。

### 5. LLM 服务最重要的延迟指标有哪些？

- 端到端延迟（E2E）：从请求进入到完整响应结束的时间。
- 首 token 时间（TTFT）：从请求进入到收到第一个输出 token 的时间，受排队、路由、预填充等影响。
- token 间延迟（ITL）或每输出 token 时间（TPOT）：解码阶段连续 token 之间的间隔。

不同应用的重点不同：交互式聊天通常重视 TTFT 和 ITL，短回答或严格超时场景更重视 E2E，长文本生成则尤其关注 ITL。

### 6. 吞吐量应该用什么指标衡量？

通用服务可使用 RPS（每秒请求数）或 RPM（每分钟请求数）；LLM 更常用 TPS，即每秒生成或处理的 token 数。TPS 更贴近 LLM 的计算消耗和按 token 计费模式，但必须明确统计口径，例如只统计输出 token，还是同时统计输入和输出 token，并保持不同方案之间口径一致。

### 7. 为什么延迟和吞吐量经常需要权衡？

提高吞吐量通常依赖批处理、排队和更高的并行度，这可能增加单个请求的等待时间；追求最低延迟则倾向于请求到达即处理，导致 GPU 并行度和利用率下降。离线批量任务通常优先吞吐量和成本，聊天机器人、实时代理通常优先 TTFT、ITL 和尾延迟。优化前应先明确 SLO，而不是笼统追求“越快越好”。

## 二、服务系统设计

### 8. 一个基本的 LLM 服务请求链路包含哪些步骤？

典型链路为：请求接入 → 认证/限流与路由 → 分词和请求构造 → 排队与调度 → 预填充/解码执行 → token 收集 → 去分词与响应映射 → 流式或完整响应返回 → 指标、日志和追踪记录。生产系统还要处理取消请求、超时、重试、错误恢复、配额和多租户隔离。

### 9. 静态批处理、动态批处理和连续批处理有什么区别？

静态批处理先固定一批请求，通常要等整批完成，简单但容易被最长请求拖慢。动态批处理在短暂等待窗口内收集请求后组批，兼顾利用率和等待时间。连续批处理以 token 迭代为调度单位，请求完成后立即让新请求加入正在运行的批次，避免等待整批结束，特别适合输出长度差异大的在线 LLM 服务。

### 10. 为什么连续批处理对解码阶段特别有效？

解码每次只生成一个 token，单个请求的 GPU 计算并不容易填满硬件；将多个请求放入同一迭代，可以一次读取模型权重并为多个序列生成 token，提高算术强度和 GPU 利用率。连续批处理还可以在某些请求完成后及时补入新请求，减少空槽和填充浪费。

### 11. 单模型服务和多模型服务如何选择？

单模型服务架构更简单，缓存和资源管理更集中，适合模型数量少、流量稳定的场景。多模型服务可以共享基础设施和降低闲置成本，但会引入模型加载、冷启动、路由、资源争用和缓存隔离问题。应根据模型数量、流量分布、SLO、模型切换频率和租户隔离要求选择，而不是默认“一套架构适合所有模型”。

### 12. 成本优化型多模型服务和延迟优化型多模型服务有什么差别？

成本优化型设计倾向于共享 GPU、按需加载或动态扩缩容，以降低闲置资源，但可能产生冷启动和排队延迟。延迟优化型设计为热门模型保留专用且常驻的资源池，能减少冷启动并提高可预测性，但成本更高。实际系统常采用混合策略：热门模型常驻，低频模型按需加载，并根据流量和 SLO 动态调整。

### 13. 自托管、云厂商托管和模型 API 应如何权衡？

模型 API 开发最快、运维负担最小，但定制能力、数据控制和成本可预测性有限。云厂商托管在易用性、弹性和控制力之间折中。自托管能控制模型、硬件、运行时、网络和优化细节，适合大规模、合规或深度性能调优场景，但需要承担 GPU、平台、监控、升级和故障处理成本。决策应综合 SLO、流量规模、数据治理、团队能力和长期总成本。

### 14. RAG、Agent 和 CAG 对 LLM 服务有什么影响？

RAG 在生成前增加检索、重排和上下文拼接，会引入额外延迟，但能让模型使用外部知识。Agent 会产生多轮 LLM 调用、工具调用和动态执行路径，容易放大尾延迟与 token 消耗。CAG 则把相对稳定的知识预加载到上下文或 KV 缓存中，减少重复检索和上下文计算，适合知识变化较慢、可接受较大缓存占用的场景。

## 三、硬件与性能瓶颈

### 15. 如何判断一次 LLM 推理是计算受限还是内存带宽受限？

看单位时间内的计算量与数据搬运量，以及硬件利用率。预填充会并行处理大量输入 token，通常更容易利用 GPU 的计算能力；解码每步只生成少量 token，却要反复读取庞大模型权重，往往受显存带宽限制。还应结合实际 profiling、输入输出长度、批大小和 GPU 利用率判断，不能只凭模型参数量下结论。

### 16. 为什么长上下文会显著增加服务成本？

长输入会增加预填充计算和 TTFT，并使 KV 缓存占用随序列长度增长。多个长上下文请求并发时，显存可能先被 KV 缓存耗尽，即使计算资源仍有余量也无法继续接收请求。工程上需要控制最大上下文、优化缓存布局、采用分页管理、前缀复用、量化或分离预填充与解码。

### 17. 为什么 GPU 显存不仅要容纳模型权重？

显存还要容纳 KV 缓存、中间激活、临时工作区、通信缓冲区、CUDA 图或内核运行所需的空间，以及批处理和调度带来的额外开销。在线服务中，KV 缓存通常随着并发数、上下文长度和生成长度变化，是决定最大并发能力的重要因素。

### 18. 为什么要区分 Prefill 和 Decode 的资源需求？

Prefill 偏大规模矩阵计算，适合高计算吞吐；Decode 具有强自回归特性，更容易受显存带宽和 KV 缓存访问影响。两者混在同一资源池中可能互相干扰：长提示的预填充会阻塞正在解码的请求，而大量解码请求也会挤占缓存。区分两阶段有助于分别调度、扩容和设定 SLO。

## 四、核心优化技术

### 19. 量化是什么？主要收益和风险是什么？

量化用更低精度表示权重、激活或 KV 缓存，例如从 FP16/BF16 降到 INT8 或更低位宽。它可以减少显存占用和内存带宽压力，使更大模型进入有限显存，并可能提高吞吐量。风险是精度损失、不同模型层敏感度差异、硬件/内核支持限制，以及量化后性能不一定自动提升，因此必须用目标任务和真实流量验证。

### 20. 什么是内核融合？为什么能加速推理？

内核融合把多个连续的小算子合并为一个 GPU kernel，减少中间张量写回、显存读写和 kernel 启动开销，并改善数据局部性。它常用于注意力、归一化和激活等热点路径。收益依赖输入形状、硬件、精度和实现，不能仅凭理论 FLOPs 判断，需通过 profiling 和端到端基准测试确认。

### 21. PagedAttention 或分页式 KV 缓存解决了什么问题？

传统 KV 缓存往往需要为不同序列预留连续空间，容易产生显存碎片和预留浪费。分页式管理把 KV 缓存切成固定大小的块，用页表或块表映射逻辑序列到物理块，使不同请求可以动态分配和回收空间，并提高缓存利用率。这对长上下文、高并发和连续批处理尤其重要。

### 22. 什么是前缀缓存？何时最有价值？

前缀缓存复用多个请求共享提示前缀已经计算出的 KV 状态，例如系统提示、固定文档、模板或工具定义。命中时可以跳过重复预填充，降低 TTFT 和计算成本。它最适合共享前缀稳定、重复率高的工作负载；若提示变化频繁、缓存容量不足或路由不稳定，命中率会下降，缓存维护成本可能抵消收益。

### 23. 如何提高前缀缓存命中率？

保持共享前缀的 token 序列完全一致，把稳定内容放在提示前部，把用户特有内容放在后部；对请求进行规范化，避免无意义的空格、字段顺序或模板差异；让具有相同前缀的请求尽量路由到保留相应 KV 块的实例；同时设置合理的缓存淘汰策略，并监控命中率、节省的 prefill 时间和额外显存占用。

### 24. 推测解码如何降低解码延迟？

推测解码使用更小、更快的草稿模型先生成多个候选 token，再让目标模型并行验证。目标模型接受的连续 token 可以一次推进多个生成步骤，从而减少目标模型逐 token 解码的次数。草稿模型越快、与目标模型越一致、候选长度 K 越合适，收益越大；如果接受率低，草稿开销和验证开销可能让性能变差。

### 25. 推测解码会不会改变目标模型的输出？

正确实现时不会改变目标模型的目标分布或最终采样结果。目标模型负责验证草稿 token；被拒绝的位置由目标模型的修正分布产生 token，后续不再依赖已被拒绝的候选。实际系统仍需验证采样策略、随机数处理、批处理形状和实现细节，以确保准确性与确定性要求得到满足。

### 26. 多 GPU 并行有哪些主要方式？

张量并行把单层计算切分到多个 GPU，适合模型或单层无法放入一张卡的情况，但需要频繁设备间通信。流水线并行按层或阶段切分模型，显存扩展性较好，但会引入阶段气泡和调度复杂度。数据并行运行多个完整副本，扩展请求吞吐最直接，但每个 GPU 都要容纳模型。选择时要同时考虑模型大小、通信互连、批大小、延迟目标和扩展方式。

## 五、框架、评测与实践

### 27. 为什么需要专门的 LLM 服务框架？

通用推理框架通常假设固定形状、短输入和一次性预测，难以高效处理自回归生成、变长上下文、KV 缓存、连续批处理和流式输出。专门的 LLM 框架把 token 级调度、缓存管理、量化、并行化和高效 kernel 组合起来，提供面向生成式工作负载的吞吐量、延迟和显存效率。

### 28. vLLM、TensorRT-LLM、SGLang 和 llama.cpp 应如何比较？

不能只按排行榜选择，应按模型、硬件和工作负载实测。vLLM 通用性强、生态成熟，适合快速构建高吞吐服务；TensorRT-LLM 深度利用 NVIDIA 软硬件，适合追求极致性能并接受较强工程约束的场景；SGLang 强调结构化生成、程序化调用和高效缓存；llama.cpp 轻量、便于 CPU/边缘或消费级硬件部署。最终应比较相同模型、精度、上下文、流量、SLO 和成本下的端到端结果。

### 29. 一次公平的 LLM 性能基准测试应如何设计？

先定义 SLO 和目标指标，再使用相同模型、精度/量化、硬件、上下文长度、输出长度、并发模式和采样参数。流量应接近真实场景，至少覆盖空闲、稳态、突发和高并发；同时记录 E2E、TTFT、ITL、TPS、p50/p95/p99、GPU 显存、功耗、错误率、冷启动和成本。必须区分 warm-up 与正式测量，重复多次，并避免通过极短输入、关闭真实排队或只报峰值吞吐量来夸大结果。

### 30. 一个可靠的 LLM 优化项目应遵循什么顺序？

先明确业务目标、SLO、成本约束和真实流量，再建立可重复的基线；随后定位瓶颈属于模型、KV 缓存、调度、硬件、网络还是应用链路；一次只改变少量变量，依次验证批处理/连续批处理、量化、缓存、内核、并行化、推测解码等技术；每一步都记录准确性、延迟、吞吐量、显存和成本。最后做回归、压力、扩缩容和故障测试，并把配置、数据集、版本和结论固化，避免只优化实验室中的单一数字。

## 六、模型结构与推理细节

### 31. Transformer 为什么适合并行处理输入提示？

在预填充阶段，同一序列中各位置的输入 token 可以通过矩阵运算并行计算；训练和预填充不需要像自回归解码那样等待前一个输出 token。因此，Transformer 的大规模矩阵乘法能够充分利用 GPU 的并行计算能力。

### 32. 为什么解码阶段无法像预填充一样完全并行？

自回归生成要求第 N 个 token 依赖前面已经生成的 token。虽然一个批次中的多个请求可以并行解码，但同一个请求的后续 token 仍需按顺序生成，这使解码天然存在串行路径，并且每一步的有效计算量相对较小。

### 33. 编码器模型、解码器模型和编码器-解码器模型有什么区别？

编码器模型主要把输入转换为上下文表示，适合分类、匹配和理解任务；解码器模型使用因果注意力，适合自回归文本生成；编码器-解码器模型先编码输入，再由解码器生成输出，适合翻译、摘要等序列到序列任务。三者的缓存、计算图和服务接口并不完全相同。

### 34. 因果注意力为什么是生成式 LLM 的基础？

因果注意力限制当前位置只能访问当前位置及之前的 token，不能偷看未来信息。这样训练时可以模拟逐 token 生成，并保证推理时每个 token 的条件分布与训练目标一致。

### 35. 注意力机制中的 Q、K、V 分别是什么？

Query 表示当前位置希望查询什么，Key 表示每个历史位置具有什么可匹配特征，Value 表示被加权汇总的内容。注意力先用 Q 与 K 计算相关性，再用相关性权重汇总 V。KV 缓存保存历史位置的 K 和 V，因为它们在后续解码步骤中可以复用。

### 36. 多头注意力为什么需要多个头？

不同注意力头可以在不同表示子空间中捕获关系，例如局部语法、长距离依赖或实体关系。多个头并行计算后再合并，使模型具备更丰富的上下文建模能力；服务优化时则要考虑头数、头维度和 KV 缓存布局。

### 37. MQA 和 GQA 为什么能降低 KV 缓存开销？

多查询注意力（MQA）让多个 query 头共享一组 key/value；分组查询注意力（GQA）让一组 query 头共享一组 K/V，介于 MHA 和 MQA 之间。由于 K/V 头数量减少，KV 缓存占用和带宽压力下降，通常有利于推理吞吐量，同时需要评估对模型质量的影响。

### 38. 模型参数量越大，推理一定越慢吗？

不一定。推理速度还取决于量化精度、硬件、批大小、上下文长度、并行方式、内核实现、缓存命中率以及输入输出比例。参数量通常决定权重存储和基础计算规模，但不能替代真实工作负载下的端到端测量。

### 39. 上下文长度和生成长度分别影响哪些阶段？

上下文长度主要增加预填充计算和初始 KV 缓存；生成长度会增加解码步数、KV 缓存增长以及最终响应时间。长输入短输出通常偏 prefill 瓶颈，短输入长输出通常偏 decode 瓶颈，二者都长时会同时消耗计算、带宽和显存。

### 40. 为什么 padding 会浪费 LLM 推理资源？

静态批处理通常需要把不同长度的序列补齐到同一长度，填充 token 不产生有用输出，却仍可能参与计算和占用缓存。请求长度差异越大，浪费越明显。连续批处理、变长序列支持和按 token 调度可以减少这种浪费。

## 七、显存、缓存与调度

### 41. 如何粗略估算模型权重的显存占用？

可以用参数数量乘以每个参数的字节数进行初步估算，例如 FP16/BF16 约为每参数 2 字节，INT8 约为每参数 1 字节，实际还要加上量化元数据、临时缓冲、框架开销和其他状态。这个估算只能判断模型是否大致放得下，不能直接推导最大并发数。

### 42. KV 缓存大小主要由哪些因素决定？

它通常与层数、KV 头数、每头维度、数据类型、序列长度和并发序列数成正比。不同架构的 KV 头配置会显著影响占用；在服务端，还要把预留块、碎片、并行副本和框架管理开销纳入预算。

### 43. 为什么显存碎片会降低最大并发？

即使总剩余显存足够，如果空闲空间被切成许多不连续的小块，也可能无法满足新序列或 KV 块的分配请求。分页式缓存和固定大小块管理可以降低外部碎片；合理的缓存淘汰和请求回收也有助于稳定可用容量。

### 44. KV 缓存何时应该被淘汰？

当缓存容量不足、请求已完成或取消、租户/会话过期、缓存内容命中率长期很低，或需要为更高优先级请求腾出空间时，都可能触发淘汰。策略可以结合最近最少使用、前缀长度、命中收益、租户配额和请求优先级，而不是只按时间淘汰。

### 45. 请求取消后为什么要及时回收资源？

用户断开连接、超时或上游取消后，如果仍保留 KV 缓存和调度槽位，系统会继续消耗 GPU 计算并降低其他请求的可用容量。取消处理应贯穿网关、调度器、执行引擎和流式响应链路，确保计算和缓存都能尽快终止或回收。

### 46. 如何设置最大并发数？

最大并发数不应只看 GPU 数量，而应根据权重占用、KV 缓存预算、上下文和输出长度分布、目标 TTFT/ITL、排队策略及尾延迟 SLO 通过压测确定。过高会造成显存不足、排队增长和 p99 恶化；过低则浪费吞吐能力。

### 47. 请求队列为什么会导致尾延迟突然变差？

当到达速率接近或超过服务能力时，队列长度会快速增长，新增请求不仅等待执行，还会受到前面长请求的影响。LLM 请求服务时间差异大，长输出会进一步拖慢队列。应监控队列等待时间、服务时间和到达率，并配合限流、优先级和自动扩缩容。

### 48. 优先级调度适合哪些场景？

适合存在不同 SLO、租户等级、交互式与离线任务混合，或系统需要保护关键请求的场景。高优先级请求可以获得更短等待或更多资源，但必须防止低优先级请求长期饥饿，并明确不同优先级对吞吐、成本和公平性的影响。

### 49. 如何处理超长请求？

可以设置输入和输出 token 上限，拒绝或降级超限请求，先摘要再推理，采用分块处理、检索、滑动窗口或专门的长上下文模型。不能只提高上下文上限，因为这会同时增加 prefill、KV 缓存、排队和成本压力。

### 50. 为什么需要对输入输出 token 做配额？

token 数量直接影响计算、显存、延迟和成本。按租户、用户、API key 或时间窗口设置配额，可以防止单一客户占满资源，并让成本和容量规划更可控。配额应与并发数、请求速率和最大上下文联合设计。

## 八、批处理、路由与缓存策略

### 51. 批大小越大，吞吐量一定越高吗？

不一定。批大小增加通常提升并行度，但会受到显存、KV 缓存、调度等待、通信和内核效率限制；过大还会增加 TTFT 和尾延迟。应测量吞吐量随批大小变化的曲线，并根据在线或离线 SLO 选择工作点。

### 52. 为什么请求长度差异会影响批处理效率？

短请求可能很快完成，长请求却持续占用批次槽位；静态批处理还会因 padding 计算无效 token。连续批处理和按 token/序列状态调度能缓解差异，但仍需要控制极端长请求对系统的影响。

### 53. 什么是 token 预算调度？

它不是只按请求个数限制批次，而是估算本轮要处理的输入和输出 token 数，并在计算预算、显存预算和延迟约束下选择请求。对于长度差异大的 LLM 流量，token 预算比固定请求数更能反映真实资源消耗。

### 54. 如何在吞吐量和 TTFT 之间做调度？

可以设置最大排队时间、最大批 token 数和调度轮次，限制为形成更大批次而等待的时间；对新请求、已进入 decode 的请求和高优先级请求分别设定预算。实际参数应通过真实流量压测，以 p95/p99 TTFT 和 TPS 共同评估。

### 55. 为什么需要请求路由，而不是简单轮询？

不同实例可能拥有不同模型、量化版本、GPU 状态、缓存内容和租户约束。简单轮询无法识别 KV 前缀命中、当前排队、显存余量和 SLO。更好的路由可以结合模型能力、缓存亲和性、区域、负载、成本、优先级和故障状态。

### 56. 什么是缓存亲和路由？

它把具有相同前缀或会话状态的请求尽量发送到保留相关 KV 缓存的实例，从而提高缓存命中率、减少 prefill 和降低 TTFT。代价是负载可能变得不均衡，因此需要在缓存收益与实例负载之间动态权衡。

### 57. 语义缓存和 KV 缓存有什么区别？

KV 缓存复用模型执行过程中产生的中间状态，要求 token 前缀通常严格匹配；语义缓存复用相似请求的最终结果或中间结果，匹配依据是语义相似度。语义缓存可能覆盖更广，但必须处理相似度阈值、答案时效性、安全隔离和错误复用风险。

### 58. 什么是请求合并，什么时候不应该合并？

请求合并是把可以共享前缀、模型或计算阶段的请求放在一起处理，以提高缓存和硬件利用率。对强实时、强隔离、输出策略差异大、数据敏感或请求取消频繁的场景，盲目合并可能增加等待、泄露状态或让调度复杂化。

### 59. 流式传输会给系统带来哪些额外要求？

服务需要维护长连接、及时刷新 token、处理客户端断开、背压、代理超时和中途取消，还要避免缓冲层把多个 token 聚集后才发送。监控应区分首 token、连续 token、断流率和完整响应成功率。

### 60. 什么时候应该使用离线批处理？

当任务不要求即时响应、输入已提前收集、输出长度相对可控且目标是降低单位 token 成本时，离线批处理通常更合适。它可以使用更大的批次、更长的等待窗口和更激进的资源利用率，但需要处理失败重试、任务持久化和结果回写。

## 九、量化、内核与分布式并行

### 61. 权重量化、激活量化和 KV 量化有什么区别？

权重量化压缩模型参数，通常直接减少模型常驻显存；激活量化压缩中间张量，可能降低计算和带宽，但对运行时支持更敏感；KV 量化压缩缓存状态，能提高长上下文并发能力，但可能影响注意力精度。三者可以组合，也可能需要分别校准和验证。

### 62. 量化校准为什么重要？

不同层、通道和 token 的数值分布不同，简单使用统一缩放因子可能造成严重误差。校准数据应尽量代表目标任务的输入分布，并检查长上下文、代码、数字和多语言等敏感模式。校准结果必须通过任务质量与服务性能共同验收。

### 63. 量化后为什么有时速度反而变慢？

如果硬件没有高效支持对应低精度格式，或量化/反量化操作引入额外开销，理论上的数据减少可能被转换和 kernel 调度成本抵消。小批次、短序列或内存瓶颈不明显时，收益也可能有限。因此应测量端到端性能，而不是只比较权重文件大小。

### 64. FlashAttention 主要优化了什么？

它通过分块计算、减少中间注意力矩阵的显存读写和提高片上存储利用率，降低注意力的内存访问开销。它不改变注意力的数学目标，但收益取决于序列长度、硬件、数据类型和 kernel 实现。

### 65. CUDA Graph 为什么可能改善推理性能？

它可以把一组稳定的 GPU 操作捕获成图，减少每步 kernel 调度和 CPU-GPU 交互开销。动态长度和动态批次会增加使用难度，通常需要对形状做分桶或准备多套图，因此要在调度灵活性和执行开销之间权衡。

### 66. 张量并行的主要瓶颈是什么？

张量并行需要在 GPU 之间进行频繁的 all-reduce、all-gather 或类似通信。若 GPU 间互连带宽不足，通信时间可能抵消计算切分带来的收益。适合的并行度取决于模型层大小、批量、序列长度、互连拓扑和目标延迟。

### 67. 流水线并行为什么会产生气泡？

不同阶段需要依次接收和传递微批次；在流水线启动和结束时，部分阶段可能没有工作，形成空闲气泡。增加微批次数可以降低气泡比例，但会增加调度和激活管理复杂度，并不一定适合低延迟在线服务。

### 68. 数据并行为什么适合扩展请求吞吐？

每个副本拥有完整模型，不同副本可独立处理不同请求，扩展方式直观且通信较少。缺点是每张 GPU 都需要存放模型权重，成本高；如果单个模型无法放入单卡，就需要结合张量或流水线并行。

### 69. 专家混合模型（MoE）会带来哪些服务挑战？

MoE 每个 token 只激活部分专家，理论上可以降低每 token 计算量，但服务还要处理专家路由、负载不均衡、跨设备通信、专家缓存和批次形状变化。总参数量不等于每 token 的实际计算量，评测必须同时关注激活参数和通信成本。

### 70. 如何选择水平扩展和垂直扩展？

垂直扩展是使用更大或更多 GPU 承载单个实例，适合模型放不下或需要减少跨实例路由；水平扩展是增加模型副本，适合扩大请求吞吐和提高故障隔离。在线服务常组合使用：先用多 GPU 放下模型，再用多副本承接流量。

## 十、框架与部署工程

### 71. vLLM 的核心工程价值是什么？

其价值不只是提供一个 HTTP 接口，而是把分页式 KV 缓存、连续批处理、token 级调度、流式生成和多种模型支持结合起来，提升显存利用率和服务吞吐。使用时仍需理解其调度参数和缓存行为，不能把它当作完全透明的黑盒。

### 72. 为什么框架 benchmark 不能直接代表生产性能？

官方或实验室 benchmark 可能使用固定输入输出长度、理想批量、无网络开销和预热后的稳定环境，而生产流量往往有突发、长尾、取消、不同租户和复杂前后处理。只有使用接近真实的请求分布和 SLO，结果才具有决策价值。

### 73. 如何选择 Triton 与专用 LLM 框架的组合方式？

Triton 擅长统一模型仓库、模型管理、协议和多模型编排；专用 LLM 引擎擅长生成式推理、KV 缓存和连续批处理。可以让专用引擎承担 LLM 核心执行，再由 Triton 或平台层统一管理，也可以根据团队运维和模型组合需求选择更简单的部署方案。

### 74. 什么时候适合使用 llama.cpp？

当目标是 CPU、消费级 GPU、边缘设备、桌面应用或低依赖本地部署时，llama.cpp 的轻量性和硬件覆盖很有价值。若目标是大规模 NVIDIA GPU 集群、极高吞吐和复杂多租户调度，则应与更偏数据中心服务的框架进行实测比较。

### 75. 模型服务的冷启动包括哪些成本？

冷启动可能包括容器启动、依赖初始化、权重下载、权重加载到 GPU、量化/编译、CUDA Graph 捕获、缓存初始化和健康检查。应分别测量每一段时间，并通过镜像预热、权重缓存、常驻副本、分层加载和合理扩缩容策略降低影响。

### 76. 自动扩缩容应使用哪些信号？

单纯使用 CPU 利用率通常不足。更有用的信号包括排队长度、排队时间、并发序列数、KV 缓存使用率、TTFT/ITL、GPU 利用率、token 到达率和预测的未来负载。扩容还要考虑冷启动时间，否则流量高峰到来时扩容可能来不及。

### 77. 如何避免扩缩容抖动？

使用稳定窗口、冷却时间、最小副本、扩容和缩容不同阈值、预测式扩容以及对排队和服务时间做平滑。缩容前应确认没有长请求、前缀缓存或会话状态需要保留，避免刚缩容就再次扩容。

### 78. 为什么模型版本和运行配置必须可追踪？

模型权重、量化方法、框架版本、GPU 驱动、批处理参数、采样参数和提示模板都可能影响质量与性能。如果只记录服务名称，出现回归时无法定位原因。应将这些信息写入部署元数据、指标标签或实验记录，并与每次 benchmark 绑定。

### 79. 多租户 LLM 服务需要隔离哪些资源？

需要考虑请求速率、并发数、token 配额、优先级、缓存、日志、数据访问、模型能力和成本归属。共享 KV 或语义缓存时必须防止跨租户复用敏感内容；资源隔离可以是逻辑限流，也可以是独立副本、GPU 分区或网络隔离。

### 80. 为什么模型服务需要健康检查之外的就绪检查？

进程存活不代表模型已经加载、GPU 可用、缓存足够、路由已注册或能够满足目标 SLO。就绪检查应验证模型权重、关键依赖、执行引擎和必要的 warm-up 状态，避免流量被发送到尚未真正可服务的实例。

## 十一、指标、观测与可靠性

### 81. 生产环境至少应该监控哪些 LLM 指标？

应覆盖请求量、成功率、错误类型、E2E、TTFT、ITL/TPOT、p50/p95/p99、输入输出 token、TPS、队列等待、并发数、KV 缓存命中率与使用率、GPU 利用率/显存/功耗、冷启动、取消率和单位 token 成本。

### 82. 为什么只看平均延迟不够？

平均值会掩盖长尾。少量超慢请求可能严重影响用户体验、SLO 和级联调用，尤其是 Agent 多步执行时。p95、p99 以及按请求长度、租户、模型版本和流量状态分组的延迟分布更能反映生产风险。

### 83. 如何拆解一次请求的端到端延迟？

可以按网关接入、排队、路由、分词、预填充、首 token、解码、去分词、网络传输和下游处理分段打点，并用 trace 关联同一个请求。拆解后才能判断是模型慢、队列长、网络抖动还是应用编排造成的延迟。

### 84. 如何区分 GPU 利用率低是空闲还是瓶颈？

需要结合 kernel 时间、显存带宽、SM 利用率、通信时间、批大小、队列长度和 CPU 调度开销。GPU 利用率低可能意味着请求不足，也可能意味着小批量、频繁 kernel 启动、同步或数据搬运导致硬件无法有效工作。

### 85. 为什么要监控 token 级成本？

LLM 成本往往随输入和输出 token 增长，且不同模型、精度、GPU 和云区域的单位成本不同。按请求、租户、模型和 token 类型统计，可以发现长提示、重复上下文、低缓存命中和低效路由造成的成本问题。

### 86. 负载测试应覆盖哪些流量形态？

至少覆盖稳态、阶梯增长、突发流量、长短请求混合、热门与冷门模型混合、不同输入输出长度、请求取消和故障恢复。仅用固定 QPS 的均匀请求，无法暴露队列、尾延迟、缓存抖动和扩缩容问题。

### 87. 什么是回归基准？

回归基准是一组固定的模型、数据、配置和指标，用于比较代码、框架、硬件或模型版本变化前后的性能与质量。它应稳定、可重复，并包含代表性的长上下文、短请求、批量和边界场景，而不是只保留一个最好看的数字。

### 88. 如何验证优化没有损害模型质量？

对量化、缓存、采样、推测解码和并行改动，应使用离线任务集、业务评测集和必要的人工抽检比较质量。需要关注精确匹配、事实性、安全性、格式遵循、长上下文能力和不同语言/领域，而不能只验证吞吐量。

### 89. 发生 GPU OOM 时应如何定位？

先确认是权重、KV 缓存、激活、临时 buffer、并行通信还是内存碎片导致；再关联请求长度、并发数、批大小、缓存使用率和模型配置。缓解方式包括限制上下文/并发、减少缓存预算、量化、分页管理、分片或扩展 GPU，但不应只简单重启服务。

### 90. 如何设计 LLM 服务的降级策略？

可以限制最大输出、切换更小模型或更低成本模型、降低采样复杂度、关闭非关键 Agent 步骤、返回缓存结果、排队离线处理或拒绝低优先级请求。降级条件和恢复条件应明确，并记录对质量、延迟、成本与租户的影响。

## 十二、云部署、安全与未来方向

### 91. 为什么前处理和后处理也会成为瓶颈？

分词、模板渲染、检索、重排、工具调用、去分词、序列化和网络传输可能占据显著时间，尤其是在模型执行被高度优化后。若只看 GPU kernel，可能误以为系统已经达到极限。应对完整链路做 profiling，并根据实际瓶颈优化 CPU、网络和中间服务。

### 92. 如何防止提示注入影响服务系统？

需要把用户输入、检索内容、系统指令和工具参数分层处理，限制工具权限，对外部内容做隔离和校验，并在执行高风险操作前进行授权和确认。服务性能优化不能绕过安全边界，缓存和日志也要避免复用或暴露不可信指令。

### 93. RAG 系统如何降低检索带来的延迟？

可以优化索引和向量检索、减少候选数量、使用并行检索与重排、缓存热门查询和文档表示，并控制最终拼接上下文长度。不要只增加 top-k，因为更多上下文会增加 prefill 和 KV 缓存压力，可能抵消召回收益。

### 94. 如何判断 CAG 是否优于 RAG？

比较知识规模、更新频率、共享前缀比例、检索延迟、上下文成本、缓存容量和答案时效性。知识稳定、反复访问、可整体放入上下文时，CAG 可能更简单更快；知识巨大、变化频繁或查询差异大时，RAG 更灵活。

### 95. 多 LoRA 服务的主要挑战是什么？

多个 LoRA 适配器共享基础模型可以降低每个适配器的权重成本，但需要处理适配器加载、切换、缓存、批处理混合、隔离和调度。适配器数量多且请求分散时，加载开销和缓存抖动会降低收益；应根据热门程度和租户需求做分层缓存。

### 96. 多模态服务为什么比纯文本服务更复杂？

图像、音频或视频需要额外的编码器、预处理、数据传输和显存，输入大小与计算量差异更大。多模态 token 化、跨模态缓存、批处理兼容性和安全检查都会影响延迟与成本。评测应分别测量媒体处理、编码、LLM prefill 和 decode 阶段。

### 97. LLM 服务如何支持强化学习或 RLHF？

在 RLHF 等流程中，服务引擎需要为 actor 模型批量生成大量候选响应，再把结果交给奖励模型和训练流程。因此吞吐量、可扩展性、权重同步和确定性都很重要；服务端的微小非确定性可能累积为奖励和训练结果差异。

### 98. 为什么 RL 服务特别关注确定性？

如果不同副本、批次形状或运行轮次产生不一致输出，奖励计算和策略更新会出现额外噪声，导致训练不稳定且难以复现。应固定必要的随机性控制、采样配置、批处理行为和版本环境，并在性能与确定性之间做显式权衡。

### 99. 语义感知路由的价值是什么？

路由器可以根据请求主题、难度、上下文、租户或所需能力选择不同模型、LoRA、缓存或区域。例如简单请求走小模型，复杂请求走大模型；相似请求路由到已有前缀缓存的实例。它有望降低成本和延迟，但需要额外分类开销、路由质量评估和错误路由的兜底机制。

### 100. 面向未来的 LLM 服务系统应保留哪些不变原则？

无论硬件、模型和模态如何变化，都应坚持：从业务 SLO 和真实流量出发；区分 prefill 与 decode；显式管理 KV 缓存和资源；用公平基准测试验证优化；同时衡量质量、延迟、吞吐量、可靠性和成本；让模型、框架、硬件和配置可替换、可观测、可回滚。

## 复习顺序建议

1. 先掌握第 2、3、5 题：理解 Prefill、Decode、KV 缓存和指标。
2. 再掌握第 9、10、15、16、18 题：建立调度与硬件瓶颈直觉。
3. 然后学习第 19–26 题：理解常用优化手段及其适用条件。
4. 最后用第 27–30 题指导框架选择、基准测试和真实项目落地。

## 来源

- Chi Wang、Peiheng Hu：《LLM 服务与优化实战》，O'Reilly Media，2026 年中文版 PDF。
- 重点覆盖原书第 1–10 章：模型服务基础、LLM 执行机制、系统设计、服务最佳实践、服务挑战、核心优化、高级优化、服务框架、优化实践与未来进展。
