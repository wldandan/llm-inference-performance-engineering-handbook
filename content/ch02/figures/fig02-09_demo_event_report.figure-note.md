# 图 2-9 制作说明：真实 vLLM 请求生成生命周期证据报告

- 来源段落：第 2.9 节与 `code/ch02/README.md`。
- 阅读顺序：真实请求进入采集器，同时读取客户端流、请求级指标与 Prometheus 前后快照，再输出证据分级报告。
- 关键结论：真实请求是主 Demo；离线 JSONL 状态机只负责验证边界和异常路径。
- 表达边界：服务端未暴露的阶段写为 unavailable，不根据客户端时间反推 Queue 或 Prefill。
