# Chapter 4 Integration Report

## 已回填图片

- 图 4-1：LLM 推理系统的请求路径、响应路径与主要逻辑组件。
- 图 4-2：Gateway 管理协议、身份与请求规范化，Client 管理业务调用和用户侧观测。
- 图 4-3：Router 管目标选择，Admission 管准入和全局队列，Engine Scheduler 管实例内执行计划。
- 图 4-4：Worker 持有模型执行状态，Runtime 组织执行路径，Accelerator 承担计算和设备内存压力。
- 图 4-5：请求、执行和资源状态由不同组件拥有，但调度与清理需要它们协同。
- 图 4-6：从单实例到平台化服务，逻辑职责保持稳定，进程和故障边界逐步展开。
- 图 4-7：平台、推理服务器和 LLM Engine 覆盖不同层级，不能脱离范围直接横向比较。
- 图 4-8：架构图把现象落到组件与交接点，后续指标和工具才有明确采集位置。
- 图 4-9：Demo 把组件与连线转成可验证的请求路径、响应路径和部署边界。
- 图 4-10：Chapter 4 定义组件责任和交接关系，模型计算、硬件、指标与调度优化由后续章节展开。

## 修改位置

- 图 4-1 位于核心问题之后，作为全章架构总览。
- 图 4-2 位于 4.2 Client 与 Gateway 职责清单之后。
- 图 4-3 位于 4.3 三类流量决策对照表之后。
- 图 4-4 位于 4.4 Worker / Runtime / Accelerator 信息与产出表之后。
- 图 4-5 位于 4.5 三类状态所有权表与取消传播说明之后。
- 图 4-6 位于 4.6.2 单实例到平台化的四步演进之后。
- 图 4-7 位于 4.6.3 三层级对照表之后。
- 图 4-8 位于 4.7 架构图到可观测边界的说明之后。
- 图 4-9 位于 4.8 Demo 的 profile 与错误连线说明之后。
- 图 4-10 位于 4.10 常见误区之后，作为章节边界收口。

## 图号与路径检查

正文只包含图 4-1 到图 4-10，编号连续无跳号；10 个 SVG 路径、10 份 figure-note 与 `storyboard.md` 定义一致，`content/ch04/figures/test_figures.py` 的 3 项检查与 10 项 subtest 通过。

## 图文一致性

- 图 4-1 的最小路径与正文一致为单实例形态，Router 与全局 Admission 画成可选扩展，不画成所有部署都存在的独立组件。
- 图 4-2 把协议处理与路由分开，不把"请求是否合法"与"合法请求去哪里"混成一个框。
- 图 4-3 三个决策各有独立输入与输出，不合并成一个模糊的"调度器"。
- 图 4-4 区分 Worker（软件执行单元）与 Accelerator（硬件），不把两者画成同一个框。
- 图 4-5 标出状态的主要拥有者，同时留出缓存副本的位置，不暗示状态只存在一份。
- 图 4-6 的演进四步只改变部署与故障边界，逻辑角色保持不变。
- 图 4-7 三个层级不做产品优劣排序，只标覆盖范围。
- 图 4-9 表达契约检查器验证的是声明自洽性，不表达它能发现真实进程。

## 未解决问题

正文与图只直接证明 Client、Gateway 与 Accelerator 三段链路；Engine Scheduler、Worker 与 Runtime 的进程边界来自 vLLM 架构说明，尚无本课程真机证据。第 2 章完整生命周期报告生成后，可为 request ID、Queue 与请求终态补充直接观测。

## 验收结果

正文、storyboard、SVG、figure-note、图号与路径已经对齐；插图契约测试通过。逻辑角色与运行态证据的边界已在正文与图中分别标明。
